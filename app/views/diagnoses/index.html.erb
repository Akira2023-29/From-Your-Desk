<div class="container mx-auto text-center justify-center">
  <div class="mb-2 flex flex-col">
    <div class="mb-1">
      <h1 class="font-bold text-4xl"><%= t('.title') %></h1>
    </div>

    <!-- タグ別検索フォーム -->
    <div class="p-2 my-2 bg-green-800 rounded-md">
      <div class="flex justify-around">
        <div class="font-bold text-white">
          <%= t('.tag_search') %>
        </div>
        <% Tag.all.each do |tag| %>
          <%= link_to tag.tag_name, tagged_diagnoses_path(tag_name: tag.tag_name), class: "font-bold mb-2 rounded px-2 bg-stone-200 hover:bg-teal-500 transition duration-150 ease-in-out" %>
        <% end %>
      </div>
      <!-- グラフ -->
      <%= render "shared/graph" %>
    </div>
  </div>

  <div class="p-4 mb-2 bg-green-800 rounded-md">
    <div class="mb-3 flex flex-wrap justify-center">
      <% if @diagnoses.present? %>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
          <%= render @diagnoses %>
        </div>
      <% else %>
        <div><%= t('.not_result') %></div>
      <% end %>
    </div>
    <%= paginate @diagnoses, theme: 'diagnoses' %>
  </div>
</div>
